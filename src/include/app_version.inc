; ======================================================
; Application version. Bump APP_VERSION here on a release.
; Two numbers by default ("major.minor"); a patch number may be appended
; for fixes, e.g. "0.1.12". Plain text string used in the load/exit banner.
; (The build date/time comes from build/buildinfo.inc, generated by the Makefile.)
; ======================================================

	DEFINE APP_VERSION "0.1.11"
